<%= form_for @review, url: {action: "create"}, html: {class: "nifty_form"} do |f| %>
    
  <% if @review.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>
 
    <ul>
    <% @review.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
  

  <%= f.label :film_id %><br>
  <%= f.collection_select(:film_id, Film.all, :id, :title) %><br>
  
  <%= f.label :number_of_stars %><br>
  <% (1..10).each do |star| %>
    <%= f.label star %> 
    <%= f.radio_button(:number_of_stars, star) %>
  <% end %>



  <br><br>
  <%= f.label :comment %><br>
  <%= f.text_area :comment, size: "60x12" %><br>
  <%= f.label :author %><br>
  <%= f.text_field :author %><br>
  
  <%= f.submit "create" %>
<% end %>

<%#= link_to 'Films', films_path %>

